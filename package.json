{
  "name": "toon-learning-system",
  "version": "1.0.0",
  "description": "Sistema interativo para aprender e usar TOON (Token-Oriented Object Notation)",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "npm run build:test && node --test dist/tests",
    "build:test": "tsc -p tsconfig.test.json",
    "lint": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "dependencies": {
    "next": "14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zustand": "^4.4.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "prettier": "^3.0.0",
    "tailwindcss": "^3.3.0",
    "typescript": "^5.2.0"
  }
}
